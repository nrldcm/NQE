# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
# pubspec.lock IS committed (app, not package) for reproducible CI builds.

# Platform folders are regenerated in CI via `flutter create .`
android/
ios/
linux/
macos/
windows/
web/

# IDE / OS
.idea/
.vscode/
*.iml
.DS_Store

# Secrets / keystores
*.keystore
*.jks
key.properties
